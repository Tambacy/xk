# 登录实现笔记

这个项目的登录是**整个程序里最难写对的一块** —— 它要在一个我们控制不了的
身份认证系统上，用真实浏览器事件走完「账号密码 → 图形验证码 → 二次验证 →
登记信任设备 → 落地教务系统」的全过程，还要在每一步都能被风控接受。

这份笔记记录**踩过的坑和结论**，不是代码导读。实现见 `xk_app/app/browser.py`
的 `login()`（约 400 行），回归测试见 `xk_app/test_second_factor.py`。

---

## 一、链路长什么样

```
xklogin.do  ──302──▶ id.tsinghua.edu.cn（统一身份认证）
                          │
                          ├─ 有有效会话？ ──是──▶ 直接落地，结束
                          │
                          ├─ 图形验证码页？（视风控而定）
                          │     └─ 截图显示在主窗口里，用户在主窗口输入
                          │
                          ├─ 填 #i_user / #i_pass
                          │     └─ 注意：#i_user 可能被前端设成 readonly 且已预填
                          │
                          ├─ 勾 singleLogin（「信任/单点登录」）★ 必勾
                          │
                          ├─ 提交（a[onclick*='doLogin']）
                          │     └─ ★ 两次提交之间强制间隔 ≥10 秒
                          │
                          ├─ 落地判定（_landed）
                          │     ├─ ok           → 完成
                          │     ├─ need_human   → 二次验证
                          │     └─ 失败          → 按页面文字分类报错
                          │
                          └─ 二次验证（三步，见第四节）
```

---

## 二、系统长什么样（实测事实）

写死这些值的地方在 `browser.py` 顶部：

```python
BASE    = "http://zhjwxk.cic.tsinghua.edu.cn/"
XKLOGIN = BASE + "xklogin.do"
XK_HOST = "zhjwxk.cic.tsinghua.edu.cn"    # 教务系统
SSO_HOST = "id.tsinghua.edu.cn"           # 统一身份认证
```

登录表单：

| 元素 | 选择器 | 备注 |
|---|---|---|
| 账号 | `#i_user` | **可能为 readonly 且已预填** |
| 密码 | `#i_pass` | |
| 信任设备 | `#theform input[name="singleLogin"]` | |
| 提交 | `a[onclick*='doLogin']` | 不是 `<button>`，是带 onclick 的 `<a>` |

二次验证页（取自前端 `doubleAuth.bundle.js`）：

```html
<input name="vericode" id="vericode" maxlength="6" inputmode="numeric">
<button type="submit">确认</button>
<a class="text-muted">…重新发送…</a>       <!-- 带倒计时 -->
```

**这套页面是 React 单页应用，元素是异步渲染的**。用固定延时去等会时灵时不灵，
必须轮询等待（见 `_wait_method_page` / `_wait_code_input` / `_find_submit_button`）。

---

## 三、踩过的坑

### 1. 用系统 Edge 会被要求二次认证，内置 Chromium 不会

最反直觉的一条。同一台机器、同一个账号，用系统 Edge 登录被判为新设备、
直接要求二次验证；Playwright 自带的 Chromium 不会。

**这是安装包必须内置一整个 Chromium（334 MB）的唯一原因。**
换句话说，那 300 多兆不是为了「有个浏览器」，是为了**避开风控**。

### 2. 短时间内连续提交登录 = 自找 sso_fail

实测：连续快速提交，轻则 `sso_fail`（票据校验失败），重则把风控要求升级成
图形验证码或二次认证。

所以 `_respect_sso_cooldown()` 在**每次提交前**强制间隔：

```python
gap = 10.0      # 秒
```

日志会写明「距上次提交登录才 N 秒，为避免被当成异常登录，等 M 秒再提交」——
这条日志不是废话，是在解释程序为什么突然停下来。

> 调试登录时最容易犯的错就是反复重跑。跑三次就要等半分钟，
> 而且越跑要求越高。

### 3. `#i_user` 可能被设成 readonly

真实页面里有这么一段前端逻辑：

```js
if ($("#i_user").val() != "") { $("#i_user").attr("readonly", "readonly"); }
```

账号一旦被预填，输入框就变只读。这时再去 `fill()` / `type()` 会**直接抛
「元素不可编辑」**，把一次本该成功的登录搞失败。

做法：先读 `readonly` 属性和当前值，**只读且非空就跳过输入**。

### 4. 「信任/单点登录」勾不上 = 每次都被当新设备

`singleLogin` 勾不上时，程序**必须出声**：日志里写清楚「少了它下次登录很可能被
要求二次认证」。

这里以前是静默 `pass`，出了问题日志里一个字都没有 —— 排查了半天才发现
是勾选框选择器变了。

### 5. 二次验证其实有三步，一开始只做了中间一步

最初的实现只做到「输入验证码」。结果**验证码根本发不出来**，因为前面还有一步
「选择验证方式」（短信 / 微信 / 其他）没做。

完整三步：

1. **选择验证方式** —— 一排 radio，必须点一个并提交，学校才会发码
2. **输入验证码** —— 6 位数字
3. **是否登记信任设备** —— 选「是」则 180 天内本机免验证码

`test_second_factor.py` 就是为这三步写的回归测试。

### 6. 第三步选「否」，学校会**立刻再问一遍**

这不是 bug，是正常现象。程序必须认得出来，否则会卡在那儿等一个永远不会来的跳转。

处理：走完一次完整的二次验证后如果还在验证页，先给学校 12 秒让它自己跳
（`redirect2Jsp`）；跳过去就直接进系统。选「否」的情况下，如实告诉用户
「这次登录到此为止，下次还会要验证码 —— 想免验证码就在最后那一步选『是』」，
而不是报一个含糊的错误。

### 7. 会话失效不是 401，是 **HTTP 200 + 一个 355 字节的小页面**

不能靠状态码判断登录状态。`_landed()` 的判据是三重：

```python
if "sso_fail" in u or SSO_HOST in u:      return False   # 还在认证域
if XK_HOST not in u:                      return False   # 不在教务域
if self._is_second_factor_page():         return False   # 还在验证页
if len(self.page.content()) < 1500:       return False   # 登陆超时小页面
```

最后那条 `< 1500` 就是为这个坑写的。

### 8. 登录成功后学校会自己跳转，这时重走登录会踩空

登记完信任设备后，学校那边会执行跳转。如果这时因为别的原因重走登录流程，
可能落在一个**没有登录表单**的页面上（中转页，或者其实已经登录了）。

硬填 `#i_user` 会抛 15 秒超时，把一次本该成功的登录搞失败。

做法：填之前先 `count() == 0` 判断，找不到表单就先问一句
`is_logged_in()`，已经登录就直接返回成功。

### 9. 窗口可能在任何等待期间被关掉

用户会把浏览器窗口关掉（或误关）。程序要能：

- 等待真人操作时发现窗口没了 → **自动重开并把窗口提到最前**，然后提示
  「窗口被关闭了，已重新打开，请在这个新窗口里继续」
- 否则用户会一直盯着屏幕等，而程序在等一个已经不存在的页面

### 10. `windowed` 模式下 `sys.stdout` 是 `None`，Playwright 会挂住

PyInstaller 的窗口模式没有控制台，`sys.stdout` / `sys.stderr` 都是 `None`。
Playwright 启动 node 子进程时会去用它们 → **直接挂起，没有任何报错**。

排查这个花了不少时间，因为现象是「程序卡住」而不是「程序报错」。

### 11. 报错要分类，不要笼统说「登录失败」

`login()` 最后按页面文字分类：

| 页面特征 | 给用户的说法 |
|---|---|
| 正文含「验证码」 | 登录要求验证码 |
| 正文含「不正确」「密码错误」 | 用户名或密码不正确 |
| URL 含 `sso_fail` | 票据校验失败（短时间内登录过于频繁，等几分钟再试） |
| 其它 | 登录失败，页面停在：`<URL>` |

**最后那条一定要带上 URL** —— 用户截图发过来时，URL 是最有用的线索。

---

## 四、二次验证的三步（实现要点）

`_solve_second_factor_here()` 在**后台页面上**完成，默认不开浏览器窗口。

### 第一步：选择验证方式

- 必须**先等页面渲染**（React 异步），等不到就当「认不出来」
- 选项是 radio，文案形如「通过…信息服务（企业微信）」「通过短信」
- 点完提交后**再等验证码输入框**；如果 12 秒内没出现，说明这个方式没成功
  （比如短信没登记），**换另一种方式重试**

### 第二步：输入验证码

- 提交按钮**可能比输入框晚渲染**，要轮询等（最多 6 秒）
- 允许重发（页面上有带倒计时的「重新发送」链接）
- 验证码 3 分钟内有效，所以超时上限按这个给

### 第三步：登记信任设备

- 页面是 radio：是 / 否
- 选「是」→ 180 天内本机免验证码。**要如实告诉用户这个后果**
- 选完必须提交，否则页面永远不跳转（学校不会自动跳）

---

## 五、凭据怎么处理

| 环节 | 做法 |
|---|---|
| 存储 | **Windows DPAPI** 加密，密钥由系统按「当前 Windows 账户 + 当前机器」派生 |
| 熵 | `b"XkHelper"`（改名前的旧值 `b"THUXkHelper"` 仍用于解密回退） |
| 明文 | **任何情况下都不落盘**。DPAPI 不可用时宁可拒绝保存 |
| 日志 | 密码、Cookie、SM2 密文写日志前统一脱敏成 `***` |
| 诊断包 | 导出时再次脱敏，且不含账号密码 |
| 遥测 | 没有 |

> **熵是密钥的一部分。** 改这个值等于换锁 —— 老用户磁盘上的 `credentials.dat`
> 会直接解不开。所以解密时是「依次尝试新旧两个熵」，不是只认新的。

---

## 六、可调参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `min_submit_gap` | 10 秒 | 两次提交登录之间的最小间隔 |
| `timeout` | 150 秒 | 等服务器响应落地 |
| `human_timeout` | 300 秒 | 等真人操作的上限 |
| `guard` | 6 次 | 整个登录循环的最大重试次数 |
| `restarts` | 2 次 | 「无头转可见窗口」的最大次数 |

失败重试的边界很清楚：**超过就直接抛错，不无限重试** —— 反复重试只会让风控
把要求升得更高。

---

## 七、要移植到别的学校，得改什么

这套逻辑本身是通用的，但下面这些是**针对具体系统**的：

- [ ] `BASE` / `XKLOGIN` / `XK_HOST` / `SSO_HOST` 四个地址
- [ ] 登录表单的选择器（`#i_user` / `#i_pass` / `singleLogin` / `doLogin`）
- [ ] 二次验证页的结构（验证码输入框、提交按钮、重发链接、方式 radio）
- [ ] 「信任设备」那一步的 radio 与文案
- [ ] 落地判定：教务系统的域名、页面最小字节数、以及内部页面的特征文字
- [ ] 会话失效的表现形式（这套是 200 + 小页面，别的系统可能是 302 或 401）
- [ ] 风控节奏（`min_submit_gap` 要按实测调，不同系统差别很大）

**最后一条最容易低估。** 间隔设小了会被判异常，设大了会错过选课窗口。
这个是靠反复实测摸出来的，没有通用值。

---

## 八、调试时的注意事项

1. **别反复重跑登录。** 每次重跑都在消耗风控额度，跑几次要求就升级了。
   调试优先用 `--selftest`（只验证浏览器能不能起来、网络通不通）。
2. **看日志里的 URL。** 失败时程序会把页面停在哪写进日志，
   绝大多数问题看 URL 就能定位。
3. **`test_second_factor.py` 不联网**，用假的页面 HTML 跑解析逻辑。
   改二次验证相关代码先跑它。
4. **`test_scheduler.py` 会真的登录**教务系统。它不在 CI 里跑（见
   `.github/workflows/ci.yml` 的注释），本地也尽量少跑。
