# 设计规范

这份文档记录界面的**设计语言**，目的是让这套视觉可以被复现 —— 换一个技术栈、
交给另一个人、或者过半年自己回头看，都能照着做出同一个东西。

数值全部来自 `xk_app/app/gui/theme.py`（唯一的令牌来源），不是凭印象写的。
改设计请改那里，再回来同步本文。

---

## 一、设计语言：一句话

> **冷静的淡紫、大留白、柔和分层投影、克制的动效。**

四条原则：

1. **单一主色**。整个界面只有一种紫，靠深浅和透明度拉出层级。绿 / 黄 / 红
   只用于状态，且**小面积**——它们不参与构图。
2. **白卡片浮在浅灰紫底上**。不用描边分隔内容，用投影和间距。
3. **字号跨度大**。页面标题 33px 与正文 13px 之间差 2.5 倍，层级一眼可辨。
4. **动效服务于「东西从哪来」**，不服务于炫技。切页是粒子化开，按钮是粒子溅出，
   统计数字是滚动 —— 每一处都在提示「发生了什么」，没有纯粹的装饰动画。

---

## 二、色板

### 主色（紫）

| 令牌 | 值 | 用途 |
|---|---|---|
| `primary_deep` | `#1B1236` | 最深，品牌区/日志底 |
| `primary_ink` | `#2A1F52` | 深色面板 |
| `primary` | `#6F5CAD` | **主色**。白字压得住，主按钮底 |
| `primary_2` | `#8B79C9` | 渐变亮端 |
| `primary_soft` | `#A99AD8` | 浅色文字 / 图标 |
| `primary_mid` | `#C6BCE6` | 分隔、装饰 |
| `primary_light` | `#E5E0F6` | 浅底（徽章、选中态） |
| `primary_tint` | `#F5F2FD` | 极浅底 |
| `primary_dark` | `#57468C` | 按下态 |

### 天幕（深色区）

| 令牌 | 值 |
|---|---|
| `sky_top` | `#3A2A6E` |
| `sky_mid` | `#241A4A` |
| `sky_bottom` | `#140D2B` |
| `sky_aurora_a` | `#A78CE6`（薰衣草） |
| `sky_aurora_b` | `#E296BE`（暮粉） |
| `sky_aurora_c` | `#786EDC`（蓝紫） |
| `sky_glow` | `#F3C9DE`（低空辉光） |

### 背景与文字

| 令牌 | 值 | 用途 |
|---|---|---|
| `bg` | `#F4F3F9` | 页面底 |
| `bg_soft` | `#EAE7F2` | 次级底 |
| `bg_deep` | `#E1DDEC` | 三级底 |
| `card` | `#FFFFFF` | 卡片 |
| `card_soft` | `#FCFBFE` | 卡片内的次级块 |
| `text` | `#1B1926` | 正文 |
| `text_dim` | `#66626F` | 次要文字 |
| `text_faint` | `#96929E` | 弱化文字 |
| `text_ghost` | `#B6B2C0` | 占位符 |

### 描边与分隔

| 令牌 | 值 |
|---|---|
| `border` | `#E4E1EE` |
| `border_soft` | `#EFEDF6` |
| `border_strong` | `#D2CDE0` |
| `hairline` | `rgba(28,20,54,7%)` |
| `hairline_dark` | `rgba(255,255,255,12%)` |
| `glass` | `rgba(255,255,255,16%)` |

### 状态色（小面积，不参与构图）

| 状态 | 主 | 浅底 | 深色面板上 |
|---|---|---|---|
| 成功 | `#3D8F6D` | `#E6F2EC` | — |
| 警告 | `#96681C` | `#F8F0DF` | `#C79433` |
| 危险 | `#B03A4A` | `#FAEAEC` | `#FF8095` |

### 日志区

深色终端块：底 `#171029`，正文 `#DAD6E6`，次要 `#6E6A82`。

---

## 三、字体

```
FONT_FAMILY = "Microsoft YaHei UI", "Microsoft YaHei", "PingFang SC",
              "Segoe UI", "Helvetica Neue", sans-serif
MONO_FAMILY = "Cascadia Mono", "Consolas", "SF Mono", "Menlo", monospace
```

### 层级

| 角色 | 字号 | 字重 | 说明 |
|---|---|---|---|
| 品牌大标题 `BrandTitle` | 40px | 700 | 只在登录页左栏 |
| 页面标题 `PageTitle` | 33px | 700 | 每页一个 |
| 统计数值 `StatValue` | 30px | 700 | 等宽数字 |
| 状态大字 `BigState` | 27px | 700 | 监听中 / 已完成 |
| 表单标题 `FormTitle` | 26px | 700 | 登录卡片 |
| 卡片标题 `CardTitle` | 19px | 700 | |
| 品牌副题 `BrandSub` | 14.5px | 400 | |
| 章节标题 `SectionTitle` | 15.5px | 600 | |
| 字标 `Wordmark` | 15.5px | 600 | |
| 页面副题 `PageSub` | 14px | 400 | |
| 正文 / 控件 | 13px | 400 / 600 | |
| 元信息 `NavMeta` / `EyebrowDark` | 12.5px | 600 | **字距 1.2px** |

**规矩**：

- 只有 400 / 600 / 700 三档，没有 500
- 小字号（12.5px）承担「标签」角色时加**字距**，让它读起来像标记而不是正文
- 数字（统计、时间、日志）用**等宽**，否则跳字时会左右抖

---

## 四、间距与圆角

### 圆角

| 令牌 | 值 | 用于 |
|---|---|---|
| `R_PILL` | 22px | 胶囊按钮（导航、返回、下一步） |
| `R_CARD` | 20px | 卡片 |
| `R_MD` | 14px | 中等容器 |
| `R_IN` | 11px | **输入框** |
| `R_SM` | 8px | 小块 / 徽章 |

### 高度

| 令牌 | 值 |
|---|---|
| `HERO_H` | 96px（顶部天幕不含化开区） |
| `NAV_H` | 44px（导航胶囊） |
| `RAIL_H` | 30px（步骤导轨） |
| `HeroBand.FADE` | 46px（天幕底部化开进页面的高度） |

顶部整条带子 = 96 + 46 = **142px**。窗口高 760 时占 19% —— 这个数字是被
「内容不要被顶得太靠下」倒逼出来的，不是随便定的。

### 组件内边距

| 组件 | 内边距 | 内部间距 |
|---|---|---|
| `Card` | 24 / 22 / 24 / 22 | 14 |
| `ModeCard` | 24 / 22 / 24 / 22 | 10 |
| `CourseCard` | 左 16 / 上 16 / 右 18 / 下 16 | 8 |
| `StatTile` | 20 / 17 / 20 / 17 | 9 |
| `HeroBand` | 28 / 10 / 28 / 0 | 8 |
| 导航胶囊 | 7（四周） | 8 |
| 页面内容区 | 40 / 26 / 40 / 24 | 18 |

### 页面骨架

```
┌──────────────────────────────────────────┐
│ HeroBand   96                            │
│   导航胶囊居中（品牌 + 使用说明 + 学号）      │
│   步骤导轨居中（5 步，带连接线）              │
│ FADE       46   ← 渐变化开进页面            │
├──────────────────────────────────────────┤
│ 页面内容：左右 40px、上 26、下 24            │
│   QScrollArea（滚动条平时隐形）             │
└──────────────────────────────────────────┘
```

登录页例外：**没有顶部带子**，整屏是「深色品牌区 + 浅色表单区」两栏。

---

## 五、投影

分层柔投影（一大一小叠）。**淡紫主色下投影也必须偏紫**，用中性灰会在卡片边缘
泛出一圈脏灰。

`shadow_spec(level) -> (blur, dx, dy, r, g, b, alpha)`

| 层级 | blur | dx | dy | 颜色 | alpha | 用于 |
|---|---|---|---|---|---|---|
| `card` | 30 | 0 | 7 | `38,28,74` | 22 | 静置卡片 |
| `raised` | 48 | 0 | 17 | `34,24,68` | 42 | 悬停抬起 |
| `glass` | 22 | 0 | 8 | `10,6,26` | 48 | 玻璃拟态 |
| `glow` | 28 | 0 | 9 | `111,92,173` | 78 | 主按钮 |
| `glow_hi` | 38 | 0 | 14 | `111,92,173` | 120 | 主按钮悬停 |
| `panel` | 40 | 0 | 14 | `12,8,30` | 62 | 深色面板 |

悬停时在 `card` 与 `raised` 之间切换。**过渡靠动画改 blur/alpha，不是切换图片。**

---

## 六、组件规格

### 按钮

| 类型 | 底 | 字 | 内边距 | 圆角 | 投影 |
|---|---|---|---|---|---|
| 主按钮 | `primary` → `primary_2` 竖渐变 | 白，600 | 11 / 28 | `R_MD` | `glow` |
| 主按钮悬停 | 同上，更亮 | 白 | — | — | `glow_hi` + 上移 1px |
| 次级 | `primary_light` | `primary_dark` | 11 / 22 | `R_MD` | 无 |
| 危险 | `danger_light` | `danger` | 11 / 22 | `R_MD` | 无 |
| 幽灵（导航） | 透明 | 白 76% | 4 / 14 | 13px | 无 |

所有按钮 `min-height ≥ 22px`，主按钮 44px。

### 卡片

- 白底、`R_CARD` 圆角、`card` 投影
- 内边距 24 / 22，内部间距 14
- **不描边** —— 靠投影和白底与浅灰紫页面对比出来
- 卡片标题 19px / 700

### 输入框

- 底 `card_soft`，`R_IN` 圆角，`border` 描边
- 内边距 9 / 14，高度约 40
- 聚焦时描边换 `primary`，另加一圈极浅的 `primary_tint` 光晕
- 占位符 `text_ghost`

### 状态徽章

小圆角块（`R_SM`），浅底 + 深字：
「已定位」`accent_light` / `accent`，「需补充」`warn_light` / `warn`，
「时间冲突」`warn_light` / `warn`，「未找到」`danger_light` / `danger`。

### 统计块 `StatTile`

- 白底、`R_CARD`、内边距 20 / 17
- 顶部一行：小圆点（4px）+ 标签 12.5px / `text_dim`
- 下方数值 30px / 700 / 等宽

### 步骤导轨 `StepRail`

- 节点直径 24px，圆点 → 文字间距 9px，文字 → 下个圆点 28px
- 已完成的节点：`primary` 实心 + 白色对勾
- 当前节点：`primary` 实心 + 白色序号 + 外圈光晕
- 未到达：`bg_deep` 空心 + `text_faint` 序号
- 节点之间用 1px `border` 连接线

### 课程卡片 `CourseCard`

**左侧一道 4px 状态色条**（`accent` / `warn` / `danger`），一眼看出校验结果。
这是全界面唯一使用大面积状态色的地方。

---

## 七、背景天幕

程序化的夜空，**不依赖任何外部图片**（可换成照片：把图放到
`assets/backdrop.jpg|png|webp` 即自动覆盖）。

由四层叠成，全部用 `QPainter` 现画：

1. **竖向渐变底**：`sky_top` → `sky_mid` → `sky_bottom`
2. **极光带**：三段椭圆，颜色取 `sky_aurora_a/b/c`，按不同透明度叠加、缓慢漂移
3. **星点**：随机分布，半径 1~2px，透明度随机
4. **同心弧纹**：`sky_glow` 的细弧线，极低透明度

### 五个变体

页面与变体的对应关系写在 `main_window.py` 的 `SKY_VARIANTS`：

```python
SKY_VARIANTS = ["rose", "cool", "warm", "violet", "deep"]
```

| 页面 | 变体 | 性格 |
|---|---|---|
| 登录 | `rose` | 偏粉、柔，配左侧品牌区 |
| 选择模式 | `cool` | 蓝紫更重、星更多 |
| 预定课程 | `warm` | 暮粉当主角 |
| 确认启动 | `violet` | 均衡紫罗兰（默认） |
| 运行监控 | `deep` | 压得更暗、星更密（要盯着看的一页） |

变体参数：`ys_band`（极光带位置与尺度）、`ys_panel`、`alphas`（三层透明度）、
`stars`（星点密度倍率）、`top_shift`（整体上移量），以及可选的
`c1/c2/c3/glow` 颜色覆盖与 `glow_a` 辉光强度。

---

## 八、动效

**统一缓动**：`cubic-bezier(.22, 1, .36, 1)`（HTML 侧）；Qt 侧用
`QEasingCurve.OutCubic`。都是「快出慢收」，没有回弹。

| 场景 | 时长 | 实现 |
|---|---|---|
| 页面切换（粒子渐变揭示） | 620ms | 新页从中心以径向渐变化开 |
| 卡片入场 | 420ms | 依次浮起 |
| 悬停抬起 | 230ms | 投影加强 + 上移 |
| 数据入场 | 340ms | |
| 统计数字滚动 | 520ms | 旧值滚到新值 |
| 粒子层 | 60fps | 按钮悬停/点击溅出，受重力回落 |

### 页面切换：粒子渐变揭示

新页面从**中心**以圆形化开，边缘跑一圈粒子。参数：

- `DURATION = 620` ms
- `BAND = 0.19` —— 羽化带占半径的比例。**别调大**，0.42 时整屏糊成一团雾
- `MASK_DIV = 3` —— 遮罩按 1/3 分辨率绘制，径向渐变放大不会糊，省十几倍开销
- 粒子沿环带分布，随揭示半径外扩

### 硬性排队

- 停止 / 结束后，所有计时类动画必须**冻结**，不能继续累加
- 重复推送同一份状态必须**幂等**：内容不变、行数不增长
- 动画关掉（`motion.ENABLED = False`）时，界面必须是**完整可用**的静态版本

---

## 九、HTML 演示稿（`docs/使用说明.html`）

19 屏、一页一屏的演示稿，设计语言与程序一致，但有几条自己的规矩：

- **绝不出现滚动条**。`overflow: hidden` + JS 对内容块做等比 `transform: scale()`。
  一页一屏的稿子里滚条是最伤观感的东西
- 缩放测量要量 `.slide` 的 padding box，不能量 `.inner` 的 `clientHeight`；
  末元素的 `margin-bottom` 要清零，否则每页都会虚假缩小一档
- 每屏独立背景（CSS/SVG 现画，无外链图片），切页时背景随之变化
- 全局一个粒子 canvas 贯穿所有页，切页时颜色向当前页色调过渡
- 相对单位：`clamp()` 控制内外边距，容器宽 `min(1180px, 94vw)`

关键变量：`--ease: cubic-bezier(.22,1,.36,1)`，`--dur: .78s`，
`--r-card: 20px`，`--r-pill: 999px`。

---

## 十、Qt 实现的硬约束

**这一节是复现时最容易踩的坑** —— 上面有些值在 Qt 里根本做不到：

| 想要 | Qt 的限制 | 实际做法 |
|---|---|---|
| `box-shadow` | 样式表**不支持** | 用 `QGraphicsEffect`；**一个控件只能有一个**，父子都加会互相吃掉 |
| `transition` | 样式表**不支持** | `QPropertyAnimation` / `QVariantAnimation` |
| `letter-spacing` | Qt 样式表**不支持** | `QFont.setLetterSpacing()` |
| 渐变按钮 | 样式表次要 | `QLinearGradient` + `paintEvent` 自绘 |
| 圆的羽化边缘 | 无现成 API | 低分辨率 `QRadialGradient` 遮罩 + `CompositionMode_DestinationIn` |
| 半透明自绘控件 | 默认会擦背景 | `WA_NoSystemBackground`，**但必须自己铺底**，否则未绘制处是黑的 |

另外三条实践中总结的：

1. **滚动条平时隐形**：`QScrollArea:hover` / `QScrollBar:hover` 时才显形。
   常驻滚动条会破坏留白
2. **自绘动画计时器**：`Qt.PreciseTimer` + 15ms，别用 `QTimer` 默认精度
3. **区域重绘**：揭示动画每帧只有一圈窄环在变，用 `QRegion` 相减只重画环带；
   整屏重画时开销在**后台缓冲交换**上，不在 `paintEvent` 里

---

## 十一、复现清单

要做出同一套观感，按顺序落实这些：

- [ ] 定一套单一主色，配 9 级明度（deep → tint）
- [ ] 页面底用**浅灰紫**（不是纯白），卡片用纯白
- [ ] 五级圆角：8 / 11 / 14 / 20 / 22（+ 胶囊 999）
- [ ] 字号跨度至少 2.5 倍：33px 标题 vs 13px 正文
- [ ] 投影颜色**跟着主色偏**，不用中性灰
- [ ] 状态色只用在徽章和状态条，不参与构图
- [ ] 数字一律等宽
- [ ] 顶部带子高度控制在窗口的 20% 以内
- [ ] 滚动条默认隐形
- [ ] 动效统一「快出慢收」，且每一处都有信息含义
- [ ] 关掉动画后界面仍完整可用

---

## 十二、改设计时

令牌只在 `xk_app/app/gui/theme.py` 定义。**不要在页面里写死颜色和字号** ——
一旦散落，这份文档就跟不上代码了。

`xk_app/app/gui/widgets.py` 是组件几何，`backdrop.py` 是背景，
`motion.py` 是动效。这四处加起来就是全部视觉层。
