# 安全说明

## 报告安全问题

如果你发现的是**安全问题**（凭据泄露、隐私泄露、绕过认证、任意代码执行等），
**请不要开公开 Issue**。请用本仓库的私密漏洞报告渠道：

**<https://github.com/Tambacy/xk/security/advisories/new>**

作者会尽快回复。修复前请勿公开细节。

## 本项目怎么处理你的账号密码

这是最常被问到的问题，先说清楚设计：

| 环节 | 做法 |
|---|---|
| 存储 | 用 **Windows DPAPI**（`CryptProtectData`）加密后存本机，密钥由系统按「当前 Windows 账户 + 当前机器」派生 |
| 解密 | 只有同一台机器的同一个 Windows 账户能解开。换账户、换电脑、拷贝文件都解不开 |
| 传输 | **只发往学校官方的统一身份认证与选课系统**，没有第三方 |
| 日志 | 密码、Cookie、SM2 密文在写日志前统一脱敏成 `***` |
| 诊断包 | 导出时再次脱敏，且**不含**账号密码 |
| 遥测 | **没有**遥测、统计上报、远程配置、自动更新 |
| 明文 | 任何情况下都不会明文落盘 —— DPAPI 不可用时**宁可拒绝保存** |

相关实现见 `xk_app/app/secretstore.py`，回归测试见 `xk_app/test_security_logging.py`。

## 哪些问题算安全问题

欢迎报告：

- 密码、Cookie 或其它凭据被明文写入磁盘、日志或诊断包
- 诊断包导出时脱敏不完整
- 程序向学校系统之外的任何地址发送了数据
- 浏览器 profile 或登录态被错误地写进了可分发的位置
- 安装包内含有账号相关的残留数据

## 哪些不算

- **程序本身能自动化选课** —— 这是它的功能，不是漏洞
- 「用自动化工具选课违反校规」 —— 见 [免责声明](DISCLAIMER.md)，
  这属于使用范围问题，不是代码安全问题
- 依赖库（Playwright / PySide6 / Chromium）自身的漏洞 ——
  请报给上游，但欢迎在这里提一句，作者会评估是否升级

## 支持范围

只维护**最新版本**。发现的问题会在下一个版本里修复，
历史版本不再单独出补丁。

## 提交前自查

如果要在 Issue 里贴日志或诊断包，请先自己搜一遍：

```powershell
Select-String -Path "$env:LOCALAPPDATA\XkHelper\logs\*.log" -Pattern "你的学号"
```

查不到再贴。日志里的密码已经自动脱敏，但学号是明文显示的（打码只作用于界面顶部）。
